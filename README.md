An MCP server that turns Claude into a stateful job-search assistant. Paste a job posting URL into a conversation, and Claude extracts the details, saves them to MongoDB, and lets you manage your whole application pipeline by talking to it.

Built with the Model Context Protocol Python SDK.
